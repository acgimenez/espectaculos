<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>

	<ul>
		{% for post in posts %}

			<li>
				<a href="{{ path('ficha_post', {id: post.id}) }}">
					{{post.name}}
				</a>
			</li>
		
		{% endfor %}
	</ul>

</body>
</html>